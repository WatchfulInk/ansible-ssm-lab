- name: Install Apache Web Server
  hosts: localhost
  become: yes

  tasks:
    - name: Install httpd
      dnf:
        name: httpd
        state: present

    - name: Start httpd service
      service:
        name: httpd
        state: started
